# M2NP

Scripts:

```
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "start": "sirv public -s",
    "format": "prettier --write --plugin-search-dir=. --svelte-bracket-new-line false ./src/*"
  },
```